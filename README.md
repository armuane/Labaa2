# Тема 3. Операторы, условия, циклы
Отчет по Теме #3 выполнил:
- Легков Иван Сергеевич
- ПИЭ-21-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

# Лабораторная работа

## Задание 1
Создайте две переменные, значение которых будете вводить через  консоль. Также составьте условие, в котором созданные ранее  переменные будут сравниваться, если условие выполняется, то  выведете в консоль «Выполняется», если нет, то «Не выполняется».

```python
one = int(input('Введите первое число '))
two = int(input('Введите второе число '))
if one >= two:
    print('Выполняется')
else:
    print('Не Выполняется')
```
  ### Результат
  ![image](https://github.com/armuane/Labaa2/blob/main/1.JPG?raw=true)


## Задание 2
Напишите программу, которая будет определять значения переменной  меньше 0, больше 0 и меньше 10 или больше 10. Это нужно  реализовать при помощи одной переменной, значение которой будет  вводится через консоль, а также при помощи конструкций if, elif, else

  ```python
one = int(input('Введите переменную '))
if one <0:
    print('Значение меньше нуля')
elif 0 < one < 10:
    print('Значение больше 0 но меньше 10')
else:
    print('Значение больше нуля')
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/2.JPG?raw=true)

## Задание 3
Напишите программу, в которой будет проверяться есть ли переменная  в указанном массиве используя логический оператор in.  Самостоятельно посмотрите, как работает программа со значениями  которых нет в массиве numbers.

  ```python
numbers = [1,2,3,4,5,6]
x = int(input('Введите переменную '))
if x in numbers:
    print('Переменная есть в этом массиве')
else:
    print('Переменной нету в этом массиве')
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/3.JPG?raw=true)

## Задание 4
Напишите программу, которая будет определять находится ли  переменная в указанном массиве и если да, то проверьте четная она или  нет. Самостоятельно протестируйте данную программу с разными  значениями переменной value

  ```python
numbers = [1,2,3,4,5,6]
x = int(input('Введите переменную '))
if x in numbers:
    if x % 2 ==0:
        print('Переменная есть в этом массиве и она четная')
    else:
        print('Переменная есть в этом массиве и она нечетная')
else:
    print('Переменной нету в этом массиве')
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/4.JPG?raw=true)



## Задание 5
Напишите программу, в которой циклом for значения переменной i  будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и  операций работают в цикле.

  ```python
for i in range(10):
    print('i = ', i)
    if i == 0:
        i += 2
    if i == 1:
        continue
    if i == 2 or i ==3:
        print('Переменная равна 2 или 3')
    elif i in [4,5,6]:
        print('Переменная равна 4,5 или 6')
    else:
        break
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/5.JPG?raw=true)



## Задание 6
Напишите программу, в которой при помощи цикла for определяется  есть ли переменная value в строке string и посмотрите, как работает  оператор else для циклов. Самостоятельно посмотрите, что выведет  программа, если значение переменной value оказалось в строке string.  Определять индекс буквы не обязательно, но если вы хотите, то это  делается при помощи строки:  index = string.find(value)  Вы берете название переменной, в которой вы хотите что-то найти,  затем применяете встроенный метод find() и в нем указываете то, что  вам нужно найти. Данная строка вернет индекс искомого объекта.

  ```python
string = 'Привет всем изучающим Python!'
x = input()
for i in string:
    if i == x:
        index = string.find(x)
        print(' Буква ', {x},' есть в строке под номером', {index})
        break
else:
    print(' Буквы ', {x},' нету в заданой строке')
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/6.JPG?raw=true)



## Задание 7
Напишите программу, в которой вы наглядно посмотрите, как работает  цикл for проходя в обратном порядке, то есть, к примеру не от 0 до 10,  а от 10 до 0. В уже готовой программе показано вычитание из 100, а  вам во время реализации программы будет необходимо придумать свой  вариант применения обратного цикла

  ```python
x = 100
for i in range(21, -1, -3):
    x -= i
    print(i, x)
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/7.JPG?raw=true)



## Задание 8
Напишите программу используя цикл while, внутри которого есть  какие-либо проверки, но быть осторожным, поскольку циклы while при неправильно написанных условиях могут становится бесконечными,  как указано в примере далее

  ```python
x = 0
while x <100:
    if x == 0:
        x += 10
    elif x // 5 > 1:
        x *= 5
    else:
        x -= 5
    print(x)
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/8.JPG?raw=true)




## Задание 9
Напишите программу с использованием вложенных циклов и одной  проверкой внутри них. Самое главное, не забудьте, что нельзя  использовать одинаковые имена итерируемых переменных, когда вы  используете вложенные циклы

  ```python
x = 0
for i in range(10):
    for j in range(10):
        if i != j:
            x += j
        else:
            pass
print(x)
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/9.JPG?raw=true)




## Задание 10
Напишите программу с использованием flag, которое будет  определять есть ли нечетное число в массиве. В данной задаче flag  выступает в роли индикатора встречи нечетного числа в исходном  массиве, четных чисел.
 ```python
x = [2,4,6,8,11]
flag = False
for value in x:
    if value % 2 == 1:
        flag = True
        y = value

if flag is True:
    print('В массиве есть нечетное число',y)
else:
    print('В массиве все числа четные')
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/10.JPG?raw=true)


# Самостоятельная работа

## Задание 1
Напишите программу, которая преобразует 1 в 31.  Для выполнения поставленной задачи необходимо обязательно и  только один раз использовать:  

• Цикл for  

• *= 5  

• += 1  

Никаких других действий или циклов использовать нельзя

  ```python
i = 1
while i < 31:
    i+=1
    if i == 5:
        i *= 5
print(i)
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/11z.JPG?raw=true)



## Задание 2
Напишите программу, которая фразу «Hello World» выводит в  обратном порядке, и каждая буква находится в одной строке консоли.

  ```python
value = "Hello World"
for i in ''.join(reversed(value)):
      print(i)
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/12z.JPG?raw=true)



## Задание 3
Напишите программу, на вход которой поступает значение из консоли,  оно должно быть числовым и в диапазоне от 0 до 10 включительно (это  необходимо учесть в программе). Если вводимое число не подходит по  требованиям, то необходимо вывести оповещение об этом в консоль и  остановить программу. Код должен вычислять в каком диапазоне  находится полученное число. Нужно учитывать три диапазона:  

• от 0 до 3 включительно  

• от 3 до 6  

• от 6 до 10 включительно  

Результатом работы программы будет выведенный в консоль диапазон.  Программа должна занимать не более 10 строчек в редакторе кода.
  ```python
value = int(input("enter: "))
if 0 < value <= 3:
      print("Диапазон 0 < value <= 3")
elif 3 < vlue < 6:
      print("Диапазон 3 < vlue < 6")
elif 6 < value <= 10:
      print("Диапазон 6 < value <= 10")
else:
      print("Не в диапазоне")
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/13z.JPG?raw=true)



## Задание 4
Манипулирование строками. Напишите программу на Python, которая  принимает предложение (на английском) в качестве входных данных  от пользователя. Выполните следующие операции и отобразите  результаты:  

• Выведите длину предложения.  

• Переведите предложение в нижний регистр.  

• Подсчитайте количество гласных (a, e, i, o, u) в предложении.  

• Замените все слова "ugly" на "beauty".  

• Проверьте, начинается ли предложение с "The" и заканчивается  ли на "end".  

Проверьте работу программы минимум на 3 предложениях, чтобы  охватить проверку всех поставленных условий.
  ```python
arr = ['a', 'e', 'i', 'o', 'u', 'y']
value = input(.Lower()
print (value)
valuel = value.replace("ugly", “beauty")
printvatuei)
print(Len(valuet))
count = 0
for i in value’

for j in arr:

if Ling:
count += 1

print (count)
if valuel[:3] == "The".lower():

print("Hauwmaerca Ha The")
else:

print ("He HawmHaetca Ha the")
print (count)
if valuet[:3] == "The".lower():
print ("Haunaetcs Ha The")
else
print ("He Haumuaerca Ha the")
if, ''join(reversed(value1[-1:-4:-1])) =
print ("Sakanuusaerca Ha end")
else
print ("He aaxawuneaetca Ha end")

"end". Lower():

```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/14_1.JPG?raw=true)
![image](https://github.com/armuane/Labaa2/blob/main/14_2.JPG?raw=true)
![image](https://github.com/armuane/Labaa2/blob/main/14_3.JPG?raw=true)



## Задание 5
Составьте программу, из фрагментов кода.
  ```python
memory = " world "
string = " hello"
values = [0, 2, 4, 6, 8, 10]
counter = 0

if counter not in values:
    while " world" not in string:
        string + "world"

    if counter > 7:
        print(memory)

while counter != 10:
    if counter < 10:
        counter += 1
        print(string)
        print(string + memory)
```
  ### Результат
![image](https://github.com/armuane/Labaa2/blob/main/15.JPG?raw=true)

# Общий вывод:
программы демонстрируют использование различных операторов, условий и циклов в языке программирования Python:

Операторы:

Используются операторы вывода (print) для вывода информации в консоль.
Применяется оператор присваивания (=) для создания переменных и сохранения значений.
Условия:

Вводятся условные конструкции if, elif и else для проверки определенных условий.
Проверяется длина предложения и выполняются действия в зависимости от результата.
Циклы:

Используется цикл for для итерации по символам строки и выполнения определенных операций.
Применяется цикл for для повторения процесса обработки нескольких предложений.
Реализуется цикл while для ввода значений от пользователя до тех пор, пока не будет введено корректное значение.
Операции со строками:

Используются методы строк, такие как lower(), replace(), startswith(), endswith(), для манипуляций с текстом.
Ввод и обработка данных:

Программы запрашивают ввод данных от пользователя с использованием input().
Предусмотрены проверки на корректность ввода, такие как обработка исключений (ValueError).
 Программы демонстрируют использование операторов, условий и циклов для обработки строк и ввода данных от пользователя в Python. Они предоставляют примеры эффективного использования различных конструкций языка для решения задач по манипуляции строками и контролю потока программы.
